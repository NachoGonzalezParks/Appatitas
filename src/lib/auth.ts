// Helpers de autenticación — se implementan en Sprint 1 (S1-01, HU-001).
// Envolverán a supabase.auth (signUp, signInWithOAuth, signOut, getSession)
// y la escritura del rol elegido en `user_roles` (RFC-002).
export {}
